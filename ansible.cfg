[defaults]
inventory = inventory.ini
# vault_password_file is intentionally NOT set here — the file exists on the
# Ansible host at ~/avpass but not in local workspaces, which breaks local
# syntax-check and ansible-lint. Pass it explicitly on the Ansible host:
#   ansible-playbook <playbook>.yaml --vault-password-file ~/avpass

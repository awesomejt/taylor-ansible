---
- name: Upgrade all packages
  become: true
  ansible.builtin.apt:
    upgrade: full
    lock_timeout: 600

- name: Clean up APT
  become: true
  ansible.builtin.apt:
    autoclean: true
    autoremove: true
    lock_timeout: 600

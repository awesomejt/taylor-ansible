---
- name: Restart ollama service
  ansible.builtin.systemd:
    name: "{{ ollama_systemd_unit }}"
    state: restarted

- name: Restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted

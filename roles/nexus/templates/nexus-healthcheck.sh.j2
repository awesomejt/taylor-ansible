#!/usr/bin/env bash
set -euo pipefail

# Simple healthcheck: uses Nexus REST status endpoint
URL="http://127.0.0.1:8081/service/rest/v1/status"
TIMEOUT=10

if curl --silent --fail --max-time $TIMEOUT "$URL" >/dev/null; then
  exit 0
else
  echo "Nexus healthcheck failed" >&2
  exit 2
fi

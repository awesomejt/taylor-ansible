---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart nexus
  ansible.builtin.systemd:
    name: nexus
    state: restarted

- name: Reload nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded

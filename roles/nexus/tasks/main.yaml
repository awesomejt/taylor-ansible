---
- name: Ensure Java is installed
  ansible.builtin.package:
    name: "{{ nexus_java_package }}"
    state: present
  become: true

- name: Ensure nexus group exists
  ansible.builtin.group:
    name: "{{ nexus_group }}"
    state: present

- name: Ensure nexus user exists
  ansible.builtin.user:
    name: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    system: true
    create_home: false
    shell: /bin/false

- name: Ensure install directory exists
  ansible.builtin.file:
    path: "{{ nexus_install_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Download Nexus archive
  ansible.builtin.get_url:
    url: "{{ nexus_download_url }}"
    dest: "/tmp/nexus.tar.gz"
    mode: '0644'
    force: false

- name: Extract Nexus archive
  ansible.builtin.unarchive:
    src: "/tmp/nexus.tar.gz"
    dest: "{{ nexus_install_dir }}"
    remote_src: true
    creates: "{{ nexus_install_dir }}/nexus"

- name: Determine extracted Nexus directory name
  ansible.builtin.command: >
    bash -lc "ls -1 {{ nexus_install_dir }} | grep '^nexus-' | sort -V | tail -n1"
  register: nexus_dir_name
  changed_when: false

- name: Set extracted nexus path fact
  ansible.builtin.set_fact:
    nexus_extracted_path: "{{ nexus_install_dir }}/{{ nexus_dir_name.stdout }}"
  when: nexus_dir_name.stdout != ''

- name: Ensure nexus data directory exists
  ansible.builtin.file:
    path: "{{ nexus_data_dir }}"
    state: directory
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0755'

- name: Optionally move existing sonatype-work to data dir and create symlink
  ansible.builtin.block:
    - name: Stop nexus if running before move
      ansible.builtin.systemd:
        name: nexus
        state: stopped
      ignore_errors: true

    - name: Check for existing sonatype-work
      ansible.builtin.stat:
        path: "{{ nexus_install_dir }}/sonatype-work"
      register: sonatype_work_stat

    - name: Move existing sonatype-work to nexus_data_dir
      ansible.builtin.command: >
        mv {{ nexus_install_dir }}/sonatype-work {{ nexus_data_dir }}
      args:
        removes: "{{ nexus_install_dir }}/sonatype-work"
      when: sonatype_work_stat.stat.exists | default(false)

    - name: Ensure symlink from install sonatype-work to data dir
      ansible.builtin.file:
        src: "{{ nexus_data_dir }}"
        dest: "{{ nexus_install_dir }}/sonatype-work"
        state: link
        force: true
      notify: Restart nexus
  when: nexus_enable_blobstore_move | bool

- name: Ensure symlink to current nexus
  ansible.builtin.file:
    src: "{{ nexus_extracted_path | default(nexus_install_dir ~ '/nexus-' ~ nexus_version) }}"
    dest: "{{ nexus_install_dir }}/nexus"
    state: link
    force: true
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"

- name: Ensure ownership of nexus directories
  ansible.builtin.file:
    path: "{{ nexus_extracted_path | default(nexus_install_dir ~ '/nexus-' ~ nexus_version) }}"
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    recurse: yes

- name: Configure nexus run user
  ansible.builtin.lineinfile:
    path: "{{ nexus_install_dir }}/nexus/bin/nexus.rc"
    regexp: '^run_as_user'
    line: "run_as_user='{{ nexus_user }}'"
    create: true

- name: Install systemd unit for nexus
  ansible.builtin.template:
    src: "nexus.service.j2"
    dest: "/etc/systemd/system/nexus.service"
    mode: '0644'
    owner: root
    group: root
  notify:
    - Reload systemd
    - Restart nexus

- name: Enable and start nexus
  ansible.builtin.systemd:
    name: nexus
    enabled: true
    state: started

# Pre-initialize admin password using common vars if not already set
- name: Check for admin init marker
  ansible.builtin.stat:
    path: "{{ nexus_data_dir }}/.ansible_nexus_admin_set"
  register: nexus_admin_marker

- name: Wait for Nexus HTTP API to be available
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/status"
    method: GET
    return_content: false
    status_code: 200
  register: nexus_up_check
  retries: 30
  delay: 10
  until: nexus_up_check.status == 200
  when: not (nexus_admin_marker.stat.exists | default(false))

- name: Check for initial admin password file
  ansible.builtin.stat:
    path: "{{ nexus_data_dir }}/admin.password"
  register: nexus_admin_pass_file
  when: not (nexus_admin_marker.stat.exists | default(false))

- name: Read initial admin password
  ansible.builtin.slurp:
    src: "{{ nexus_data_dir }}/admin.password"
  register: nexus_admin_pass_raw
  when: (nexus_admin_pass_file.stat.exists | default(false)) and not (nexus_admin_marker.stat.exists | default(false))

- name: Set initial_admin_password fact
  ansible.builtin.set_fact:
    initial_admin_password: "{{ (nexus_admin_pass_raw.content | b64decode).splitlines()[0] }}"
  when: nexus_admin_pass_raw is defined and nexus_admin_pass_raw.content is defined

- name: Attempt to change admin password (variant: newPassword)
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/security/users/admin/change-password"
    method: PUT
    user: admin
    password: "{{ initial_admin_password | default('') }}"
    force_basic_auth: true
    body: |
      {"newPassword":"{{ nexus_password }}"}
    body_format: raw
    status_code: [200, 204]
  register: nexus_change_pass_result
  changed_when: false
  failed_when: false
  when: initial_admin_password is defined and not (nexus_admin_marker.stat.exists | default(false))

- name: Attempt to change admin password (variant: password) if first attempt failed
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/security/users/admin/change-password"
    method: PUT
    user: admin
    password: "{{ initial_admin_password | default('') }}"
    force_basic_auth: true
    body: |
      {"password":"{{ nexus_password }}"}
    body_format: raw
    status_code: [200, 204]
  register: nexus_change_pass_result_2
  changed_when: false
  failed_when: false
  when: (initial_admin_password is defined) and (nexus_change_pass_result is defined and nexus_change_pass_result.status not in [200,204]) and not (nexus_admin_marker.stat.exists | default(false))

- name: Verify password change by attempting login with new password
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/status"
    method: GET
    user: "{{ nexus_username }}"
    password: "{{ nexus_password }}"
    force_basic_auth: true
    return_content: false
    status_code: 200
  register: nexus_login_verify
  retries: 3
  delay: 5
  until: nexus_login_verify.status == 200
  when: initial_admin_password is defined and not (nexus_admin_marker.stat.exists | default(false))

- name: Create admin init marker file
  ansible.builtin.file:
    path: "{{ nexus_data_dir }}/.ansible_nexus_admin_set"
    state: touch
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0600'
  when: nexus_login_verify is defined and nexus_login_verify.status == 200

# Backups: install cron job and backup script when enabled
- name: Ensure backup directory exists
  ansible.builtin.file:
    path: "{{ nexus_backup_dir }}"
    state: directory
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0755'
  when: nexus_backup_enabled | bool

- name: Install nexus backup script
  ansible.builtin.template:
    src: "backup-nexus.sh.j2"
    dest: "/usr/local/bin/backup-nexus.sh"
    mode: '0755'
    owner: root
    group: root
  when: nexus_backup_enabled | bool

- name: Install cron entry for nexus backups
  ansible.builtin.template:
    src: "cron-nexus-backup.j2"
    dest: "/etc/cron.d/nexus-backup"
    mode: '0644'
  when: nexus_backup_enabled | bool

# Simple healthcheck script
- name: Install nexus healthcheck script
  ansible.builtin.template:
    src: "nexus-healthcheck.sh.j2"
    dest: "/usr/local/bin/nexus-healthcheck.sh"
    mode: '0755'
  notify: Restart nexus

- name: Ensure curl package is present for healthcheck
  ansible.builtin.package:
    name: curl
    state: present
---
- name: Ensure Java is installed
  ansible.builtin.package:
    name: "{{ nexus_java_package }}"
    state: present
  become: true

- name: Ensure nexus group exists
  ansible.builtin.group:
    name: "{{ nexus_group }}"
    state: present

- name: Ensure nexus user exists
  ansible.builtin.user:
    name: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    system: true
    create_home: false
    shell: /bin/false

- name: Ensure install directory exists
  ansible.builtin.file:
    path: "{{ nexus_install_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Download Nexus archive
  ansible.builtin.get_url:
    url: "{{ nexus_download_url }}"
    dest: "/tmp/nexus.tar.gz"
    mode: '0644'
    force: false

- name: Extract Nexus archive
  ansible.builtin.unarchive:
    src: "/tmp/nexus.tar.gz"
    dest: "{{ nexus_install_dir }}"
    remote_src: true
    creates: "{{ nexus_install_dir }}/nexus"

- name: Determine extracted Nexus directory name
  ansible.builtin.command: >
    bash -lc "ls -1 {{ nexus_install_dir }} | grep '^nexus-' | sort -V | tail -n1"
  register: nexus_dir_name
  changed_when: false

- name: Set extracted nexus path fact
  ansible.builtin.set_fact:
    nexus_extracted_path: "{{ nexus_install_dir }}/{{ nexus_dir_name.stdout }}"
  when: nexus_dir_name.stdout != ''

- name: Ensure nexus data directory exists
  ansible.builtin.file:
    path: "{{ nexus_data_dir }}"
    state: directory
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0755'

- name: Optionally move existing sonatype-work to data dir and create symlink
  ansible.builtin.block:
    - name: Stop nexus if running before move
      ansible.builtin.systemd:
        name: nexus
        state: stopped
      ignore_errors: true

    - name: Check for existing sonatype-work
      ansible.builtin.stat:
        path: "{{ nexus_install_dir }}/sonatype-work"
      register: sonatype_work_stat

    - name: Move existing sonatype-work to nexus_data_dir
      ansible.builtin.command: >
        mv {{ nexus_install_dir }}/sonatype-work {{ nexus_data_dir }}
      args:
        removes: "{{ nexus_install_dir }}/sonatype-work"
      when: sonatype_work_stat.stat.exists | default(false)

    - name: Ensure symlink from install sonatype-work to data dir
      ansible.builtin.file:
        src: "{{ nexus_data_dir }}"
        dest: "{{ nexus_install_dir }}/sonatype-work"
        state: link
        force: yes
      notify: Restart nexus
  when: nexus_enable_blobstore_move | bool

- name: Ensure symlink to current nexus
  ansible.builtin.file:
    src: "{{ nexus_extracted_path | default(nexus_install_dir ~ '/nexus-' ~ nexus_version) }}"
    dest: "{{ nexus_install_dir }}/nexus"
    state: link
    force: true
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"

- name: Ensure ownership of nexus directories
  ansible.builtin.file:
    path: "{{ nexus_extracted_path | default(nexus_install_dir ~ '/nexus-' ~ nexus_version) }}"
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    recurse: yes

- name: Configure nexus run user
  ansible.builtin.lineinfile:
    path: "{{ nexus_install_dir }}/nexus/bin/nexus.rc"
    regexp: '^run_as_user'
    line: "run_as_user='{{ nexus_user }}'"
    create: yes

- name: Install systemd unit for nexus
  ansible.builtin.template:
    src: "nexus.service.j2"
    dest: "/etc/systemd/system/nexus.service"
    mode: '0644'
    owner: root
    group: root
  notify:
    - Reload systemd
    - Restart nexus

- name: Enable and start nexus
  ansible.builtin.systemd:
    name: nexus
    enabled: true
    state: started

# Pre-initialize admin password using common vars if not already set
- name: Check for admin init marker
  ansible.builtin.stat:
    path: "{{ nexus_data_dir }}/.ansible_nexus_admin_set"
  register: nexus_admin_marker

- name: Wait for Nexus HTTP API to be available
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/status"
    method: GET
    return_content: no
    status_code: 200
  register: nexus_up_check
  retries: 30
  delay: 10
  until: nexus_up_check.status == 200
  when: not (nexus_admin_marker.stat.exists | default(false))

- name: Check for initial admin password file
  ansible.builtin.stat:
    path: "{{ nexus_data_dir }}/admin.password"
  register: nexus_admin_pass_file
  when: not (nexus_admin_marker.stat.exists | default(false))

- name: Read initial admin password
  ansible.builtin.slurp:
    src: "{{ nexus_data_dir }}/admin.password"
  register: nexus_admin_pass_raw
  when: (nexus_admin_pass_file.stat.exists | default(false)) and not (nexus_admin_marker.stat.exists | default(false))

- name: Set initial_admin_password fact
  ansible.builtin.set_fact:
    initial_admin_password: "{{ (nexus_admin_pass_raw.content | b64decode).splitlines()[0] }}"
  when: nexus_admin_pass_raw is defined and nexus_admin_pass_raw.content is defined

- name: Attempt to change admin password (variant: newPassword)
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/security/users/admin/change-password"
    method: PUT
    user: admin
    password: "{{ initial_admin_password | default('') }}"
    force_basic_auth: true
    body: |
      {"newPassword":"{{ nexus_password }}"}
    body_format: raw
    status_code: [200, 204]
  register: nexus_change_pass_result
  changed_when: false
  failed_when: false
  when: initial_admin_password is defined and not (nexus_admin_marker.stat.exists | default(false))

- name: Attempt to change admin password (variant: password) if first attempt failed
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/security/users/admin/change-password"
    method: PUT
    user: admin
    password: "{{ initial_admin_password | default('') }}"
    force_basic_auth: true
    body: |
      {"password":"{{ nexus_password }}"}
    body_format: raw
    status_code: [200, 204]
  register: nexus_change_pass_result_2
  changed_when: false
  failed_when: false
  when: (initial_admin_password is defined) and (nexus_change_pass_result is defined and nexus_change_pass_result.status not in [200,204]) and not (nexus_admin_marker.stat.exists | default(false))

- name: Verify password change by attempting login with new password
  ansible.builtin.uri:
    url: "http://127.0.0.1:8081/service/rest/v1/status"
    method: GET
    user: "{{ nexus_username }}"
    password: "{{ nexus_password }}"
    force_basic_auth: yes
    return_content: no
    status_code: 200
  register: nexus_login_verify
  retries: 3
  delay: 5
  until: nexus_login_verify.status == 200
  when: initial_admin_password is defined and not (nexus_admin_marker.stat.exists | default(false))

- name: Create admin init marker file
  ansible.builtin.file:
    path: "{{ nexus_data_dir }}/.ansible_nexus_admin_set"
    state: touch
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0600'
  when: nexus_login_verify is defined and nexus_login_verify.status == 200

# Backups: install cron job and backup script when enabled
- name: Ensure backup directory exists
  ansible.builtin.file:
    path: "{{ nexus_backup_dir }}"
    state: directory
    owner: "{{ nexus_user }}"
    group: "{{ nexus_group }}"
    mode: '0755'
  when: nexus_backup_enabled | bool

- name: Install nexus backup script
  ansible.builtin.template:
    src: "backup-nexus.sh.j2"
    dest: "/usr/local/bin/backup-nexus.sh"
    mode: '0755'
    owner: root
    group: root
  when: nexus_backup_enabled | bool

- name: Install cron entry for nexus backups
  ansible.builtin.template:
    src: "cron-nexus-backup.j2"
    dest: "/etc/cron.d/nexus-backup"
    mode: '0644'
  when: nexus_backup_enabled | bool

# Simple healthcheck script
- name: Install nexus healthcheck script
  ansible.builtin.template:
    src: "nexus-healthcheck.sh.j2"
    dest: "/usr/local/bin/nexus-healthcheck.sh"
    mode: '0755'
  notify: Restart nexus

- name: Ensure curl package is present for healthcheck
  ansible.builtin.package:
    name: curl
    state: present


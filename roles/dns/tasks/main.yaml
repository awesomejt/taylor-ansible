- name: Install dns utils
  ansible.builtin.apt:
    name:
      - dnsutils
      - dnsmasq
    state: present
    become: true

- name: Install dns utils
  ansible.builtin.file:
    src: hosts
    dest: /etc/hosts
    state: present
    become: true

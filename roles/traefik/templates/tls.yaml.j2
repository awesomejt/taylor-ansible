tls:
  certificates:
    - certFile: /certs/{{ traefik_tls_cert_file }}
      keyFile: /certs/{{ traefik_tls_key_file }}
  stores:
    default:
      defaultCertificate:
        certFile: /certs/{{ traefik_tls_cert_file }}
        keyFile: /certs/{{ traefik_tls_key_file }}

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart docker registry service
  ansible.builtin.systemd:
    name: "{{ registry_service_name }}"
    state: restarted

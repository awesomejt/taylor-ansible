apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ k3s_bootstrap_ca_cluster_issuer_name }}
  labels:
    app.kubernetes.io/managed-by: ansible
spec:
  ca:
    secretName: {{ k3s_bootstrap_ca_secret_name }}

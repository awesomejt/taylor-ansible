---
- name: Deploy Minecraft Java Edition game servers
  hosts: "{{ target | default('minecraft') }}"
  become: true
  gather_facts: true
  vars:
    trust_ca_install_intermediate: true
  vars_files:
    - vars/common/vars.yaml
    - vars/common/secrets.yaml
  pre_tasks:
    - name: Wait for apt locks to be released
      ansible.builtin.shell: |
        while fuser /var/lib/apt/lists/lock \
                    /var/lib/dpkg/lock \
                    /var/lib/dpkg/lock-frontend \
                    /var/cache/apt/archives/lock >/dev/null 2>&1; do
          echo "Waiting for apt lock..."
          sleep 10
        done
      changed_when: false
      timeout: 600
  roles:
    - updates
    - common
    - trust-ca
    - docker
    - minecraft
